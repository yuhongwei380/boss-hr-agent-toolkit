# tests/cli/ -- tests for boss_hr unified CLI subcommands (v1.1+).
# Do NOT move existing business tests in tests/. This is a second, independent
# CLI-level test suite for the new boss_hr/cli.py entry point.
